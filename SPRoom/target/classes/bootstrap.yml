
server:
  port: 8004


spring:
  application:
    name: SPRoom
  
  cloud:
    consul:
      config:
        enabled: true
        host: 10.160.0.18
        port: 8500
        prefix: config
        defaultContext: application
        profileSeparator: '::'
        data-key: data
        format: YAML
    
      
        
